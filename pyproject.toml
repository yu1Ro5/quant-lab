[project]
name = "quant-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.0",
    "pyarrow>=20.0.0",
    "requests>=2.34.2",
    "slack-sdk>=3.33.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/quant_lab_backtest"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B"]

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pandas-stubs>=2.3.0",
    "ruff>=0.16.0",
    "types-requests>=2.33.0.20260712",
]
